# 复制为 deploy/deploy.env 后填写（该文件已被 .gitignore 忽略，不会提交）。
# deploy.sh 会读它，决定往哪台机器发布。

# 你的 ECS。SSH_HOST 可以是 IP，也可以是 ~/.ssh/config 里的别名
SSH_HOST=root@1.2.3.4
SSH_PORT=22

# 已备案的域名，用于 Nginx server_name 和签证书
DOMAIN=vouch.example.com

# 服务器上的部署根目录，一般不用改
REMOTE_ROOT=/opt/vouch
